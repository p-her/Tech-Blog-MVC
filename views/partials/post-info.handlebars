<article class="post">
  <div class="title">
      <p>{{title}}</p>

    <span>({{format_url post_url}})</span>
  </div>
  <div class="meta">
     by {{user.username}} on {{format_date created_at}}
    |

  </div>
</article>